# Ollama Modelfile Example for GGUF Models
#
# This file demonstrates how to create an Ollama model from a GGUF file
# downloaded from Hugging Face.
#
# Usage:
#   1. Download a GGUF model from Hugging Face into models/gguf/
#   2. Copy this file and adjust the FROM path and model name
#   3. Run: docker exec -it ollama-proxy-ollama-gpu-1 ollama create my-model -f /path/to/this/Modelfile
#
# Example:
#   docker exec -it ollama-proxy-ollama-gpu-1 ollama create mistral-7b-custom -f /models/modelfiles/mistral.Modelfile

# FROM: Path to your GGUF model file
# Use absolute path inside the container or relative to where ollama is running
FROM ./models/gguf/your-model-name.gguf

# TEMPLATE: Defines how the model formats conversations
# This example uses ChatML format (common for many modern models)
# Adjust based on your model's training format (ChatML, Llama2, Alpaca, etc.)
TEMPLATE """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
{{ end }}<|im_start|>assistant
{{ .Response }}<|im_end|>
"""

# Common template formats:
#
# ChatML (OpenAI, Qwen, etc.):
# <|im_start|>system\n{system}<|im_end|>\n<|im_start|>user\n{prompt}<|im_end|>\n<|im_start|>assistant\n
#
# Llama 2:
# [INST] <<SYS>>\n{system}\n<</SYS>>\n\n{prompt} [/INST]
#
# Alpaca:
# ### Instruction:\n{prompt}\n\n### Response:\n

# PARAMETER: Model runtime parameters
PARAMETER temperature 0.7        # Creativity level (0.0-2.0, default: 0.8)
PARAMETER top_p 0.9              # Nucleus sampling (0.0-1.0, default: 0.9)
PARAMETER top_k 40               # Top-k sampling (default: 40)
PARAMETER num_ctx 4096           # Context window size (default: 2048)
PARAMETER repeat_penalty 1.1     # Penalize repetition (default: 1.1)
PARAMETER num_predict 512        # Max tokens to generate (-1 = infinite)

# SYSTEM: Default system prompt
# This can be overridden by the client request
SYSTEM """Du bist ein hilfreicher KI-Assistent, der präzise und informative Antworten gibt."""

# Optional: Stop sequences
# PARAMETER stop "<|im_end|>"
# PARAMETER stop "<|endoftext|>"

# Optional: Adapter/LoRA
# ADAPTER ./path/to/lora/adapter.gguf

# License and metadata (optional but recommended)
# LICENSE "Apache 2.0"
# MESSAGE "This model is based on [Model Name] from Hugging Face"
